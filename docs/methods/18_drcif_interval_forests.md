# DrCIF / Interval Forests

The repo now tracks the interval-forest lane through the shared modern-TSC
proxy packet:

- study id: `tsc_archive_baseline_frontier_v1`
- artifacts: `artifacts/tsc_archive_baseline_frontier_v1/`
- proxy method id in that packet: `drcif_proxy`

## What It Proves

This lane is now explicit in the method-validation operating system and is
benchmarked on the shared binary dynamics corpus through an interval-feature
proxy.

The current packet is enough to justify:

- `drcif_interval_forests` moving to `implemented`

## Claim Boundary

This is not yet a claim that the repo contains a faithful DrCIF or interval
forest implementation.

What remains open:

- faithful external-method wrapping or implementation
- calibration and seed-stability checks
- broader comparison against named physics-aware witnesses

The registry therefore keeps `drcif_interval_forests` at `implemented`, not
`witness_supported`.
