# BOSS / WEASEL / TDE Dictionary Methods

The repo now tracks the dictionary-method lane through the shared modern-TSC
proxy packet:

- study id: `tsc_archive_baseline_frontier_v1`
- artifacts: `artifacts/tsc_archive_baseline_frontier_v1/`
- proxy method id in that packet: `dictionary_proxy`

## What It Proves

This lane is now explicit in the method-validation operating system and is
benchmarked on the shared binary dynamics corpus through a symbolic
bag-of-patterns style proxy.

The current packet is enough to justify:

- `dictionary_tde_family` moving to `implemented`

## Claim Boundary

This is not yet a claim that the repo contains faithful BOSS, WEASEL, or TDE
implementations.

What remains open:

- faithful external-method wrapping or implementation
- calibration and seed-stability checks
- broader comparison against named physics-aware witnesses

The registry therefore keeps `dictionary_tde_family` at `implemented`, not
`witness_supported`.
