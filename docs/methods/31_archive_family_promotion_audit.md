# Archive Family Promotion Audit

The repo now has a bounded method-level audit for the archive classifier lane:

- study id: `archive_family_promotion_audit_v1`
- artifacts: `artifacts/archive_family_promotion_audit_v1/`

## What It Proves

This packet is not another broad family narrative. It answers a sharper
question:

Which archive family member is closest to promotion right now, and what exact
bounded blocker still matters at the method level?

It combines:

- the shared archive-versus-physics witness
- the archive feature-headroom witness
- the archive backend diagnosis packet

into one method-level ranking surface.

## Current Read

The current bounded read identifies:

- closest method: `minirocket_family`
- generic-TSC family decision: `archive_family_candidate_exists`

The important correction is that this packet is no longer only a blocker
surface. The current bounded artifact says the exact MiniRocket path now has
no audited blocker on the shared archive witness set. That does not close the
whole generic-TSC story by itself, but it does mean the lane now has a real
promotion candidate instead of only wrapper-stage diagnostics.

## Claim Boundary

This is still a ranking and blocker-identification packet, not a family
promotion packet.

It is enough to improve the generic-TSC story because the lane can now say
which archive family member currently earns bounded witness support and which
member-level holdouts still remain explicitly partial.
