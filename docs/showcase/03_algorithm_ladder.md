# Algorithm Ladder Source

This source section describes the algorithm ladder and the intended interpretation of each family:

- pointwise
- windowed feature methods
- sequential Bayesian accumulation
- Kalman-bank methods
- transition-aware sequential methods

The generated report should separate:

- what assumptions each method makes
- where each method is strong
- where each method is brittle
- which methods are not fair same-sensor comparisons
